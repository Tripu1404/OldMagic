name: Compilar InstantDamageModifier sin Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Construir plugin .jar
    runs-on: ubuntu-latest

    steps:
    - name: 🧬 Clonar repositorio
      uses: actions/checkout@v3

    - name: ☕ Configurar Java
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 17

    - name: 📦 Crear carpeta de salida
      run: mkdir -p out

    - name: 🌐 Descargar Nukkit API
      run: curl -L -o nukkit.jar https://ci.opencollab.dev/job/NukkitX/job/Nukkit/job/master/lastSuccessfulBuild/artifact/target/nukkit-1.0-SNAPSHOT.jar
    - name: 🧪 Compilar código Java
      run: |
        # Compilar todas las clases del plugin
        javac -cp "nukkit.jar" -d out src/tripu1404/instantdamagemodifier/*.java

        # Copiar plugin.yml y config.yml al directorio de salida
        cp plugin.yml out/
        cp config.yml out/

    - name: 📁 Generar plugin JAR
      run: |
        cd out
        jar cf InstantDamageModifier.jar *

    - name: 🚀 Subir artefacto .jar
      uses: actions/upload-artifact@v4
      with:
        name: InstantDamageModifier
        path: out/InstantDamageModifier.jar
